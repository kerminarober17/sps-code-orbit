/**
 * SPS CODE ORBIT - Preparatory Curriculum Courses Dataset
 * ONLY the 2 Preparatory courses:
 * 1. Web Adventures — HTML & CSS (Level: Preparatory)
 * 2. JavaScript Adventures (Level: Preparatory)
 */

const { COURSES_DATA } = require('./data/courses.js');

module.exports = {
  COURSES_DATA,
  coursesData: COURSES_DATA
};
